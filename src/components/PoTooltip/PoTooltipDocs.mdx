# PoTooltip component

import { Story, ArgTypes, Source, Meta } from "@storybook/blocks";

import * as PoTooltipStories from "./PoTooltip.stories";

import dedent from "ts-dedent";

<Story of={PoTooltipStories.Tooltip} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoTooltip text="This is a long long tooltip" placement="right"
			>I have a tip</PoTooltip
		>
  `}
/>

### Props

`text` | String | Default: "",

`placement` | String | Default: "bottom" | Other options [ 'auto' | 'auto-start' | 'auto-end' | 'top' | 'top-start' | 'top-end' | 'bottom' | 'bottom-start' | 'bottom-end' | 'right' | 'right-start' | 'right-end' | 'left' | 'left-start' | 'left-end' ]

`strategy` | String | Default: "absolute" | Other options ['absolute' | 'fixed']

### Explanation

<ArgTypes of={PoTooltipStories} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    const toggleState = ref();
  `}
/>
