# Alert component

import { ArgTypes, Source, Meta } from "@storybook/blocks";

import PoAlert from "./PoAlert.vue";
import * as PoAlertStories from "./PoAlert.stories";

import dedent from "ts-dedent";

**Note: Select Alert > Alert from left side menu to see the preview**

<Source
	language="js"
	dark
	format={true}
	code={dedent`
   <PoAlert
        cancel-btn-label="Dismiss"
        ok-btn-label="Confirm"
        alert-title="I am an alert"
        alert-description="It's an alert! I repeat, an alert!!"
        @confirm="handleConfirm"
        @cancel="handleCancel"
        :show="showAlert"
        @alert-closed="() => (showAlert = false)"
    />
  `}
/>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    let showAlert = ref(false);
    function openAlertClick() {
        showAlert.value = true;
        setTimeout(() => {
            showAlert.value = false
        }, 100)
    }
    function handleAlertOkClick() {
        console.log('alert ok')
    }
  `}
/>

### Explanation

<ArgTypes of={PoAlert} />
