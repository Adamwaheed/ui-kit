# Pagination component

import { Story, ArgTypes, Source, Meta } from "@storybook/blocks";

import * as PoPaginationStories from "./PoPagination.stories";

import dedent from "ts-dedent";

<Meta of={PoPaginationStories} />

Always display this component on the right side of the page.

<div className="po-flex po-justify-end">
	<Story of={PoPaginationStories.Pagination} />
</div>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoPagination
      :totalPages="2"
      :currentPage="1"
      @next-click="handleNextlick"
      @prev-click="handlePrevClick"
      />
  `}
/>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    let pagination = {
        label: 'Page 2 of 20',
    }
    function handleNextlick() {
      ... do something
    }
    function handlePrevlick() {
      ... do something
    }
  `}
/>

This way of doing it is no longer reccomended.

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoPagination :pagination="pagination" @button-click="handlePaginationClick" />
  `}
/>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    let pagination = {
        label: 'Page 2 of 20',
    }
    function handlePaginationClick(item) {
      switch (item) {
        case "prev":
            ...
            break;
        case "next":
            ...
            break;
      }
    }
  `}
/>

Pagination also lives inside PoPageTitle component.

### Explanation

<ArgTypes of={PoPaginationStories} />
