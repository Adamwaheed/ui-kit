# Empty component

import {
  Story,
  ArgsTable,
  Description,
  Source,
  Canvas,
  Meta,
} from "@storybook/addon-docs";

import PoEmpty from "./PoEmpty.vue";

import dedent from "ts-dedent";

<Meta title="Components/Empty" component={PoEmpty} />

<Description of={PoEmpty} />

<Canvas withToolbar>
  <Story id="components-empty--empty" />
</Canvas>

<Source
  language="js"
  dark
  format={true}
  code={dedent`
   <PoEmpty
        label="Nothing to show"
        description="No show for sho, Write a helpful message. Maybe suggest something to do when it's empty."
        :icon="BeakerIcon"
    />
  `}
/>

### Explanation

<ArgsTable of={PoEmpty} />
