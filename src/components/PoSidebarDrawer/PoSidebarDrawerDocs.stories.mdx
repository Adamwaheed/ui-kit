# Sidebar drawer component

import {
	Story,
	ArgsTable,
	Description,
	Source,
	Canvas,
	Meta,
} from "@storybook/addon-docs";

import PoSidebarDrawer from "./PoSidebarDrawer.vue";

import dedent from "ts-dedent";

<Meta title="Shell/Sidebar Drawer" component={PoSidebarDrawer} />

<Description of={PoSidebarDrawer} />

<Canvas withToolbar>
	<Story id="shell-sidebar-drawer--sidebar-drawer" />
</Canvas>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoSidebarDrawer
			:content="sidebarContent"
      :curr-route="currRoute"
			@button-click="handleSidebarButtonClick"
			:has-feedback="true"
			:apps="appsFromApi"
			app-code="auth"
		/>
  `}
/>

The property `app-code` is the apps code from API. It's used to filter related apps.

### Explanation

<ArgsTable of={PoSidebarDrawer} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    let sidebarContent = ref([
      {
        groupName: "Call Centre",
        items: [
          {
            label: "Dashboard",
            url: "/callcentre",
            icon: PresentationChartLineIcon,
          },
          {
            label: "Queue",
            url: "/callcentre/queue",
            icon: QueueListIcon,
            disabled: true
          },
          ...
        ],
      },
      ...
    ]);
    function handleSidebarButtonClick(link) {
        router.push(link);
    }
  `}
/>

You can optionally pass a `disabled: true`

### Feedback button

To add feedback button to sidebar, you can use the prop `:has-feedback="true"` and handle the click using `@button-click`. Button click will give the value `feedback-button`.
