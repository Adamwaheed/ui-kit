# Description List component

import { Story, ArgTypes, Source, Meta } from "@storybook/blocks";

import * as PoDescriptionListStories from "./PoDescriptionList.stories";

import dedent from "ts-dedent";

### List

<Story of={PoDescriptionListStories.List} />

### With action

Action click just returns the string passed `action: "Update",`. If you want to add more functionality, and have it return an object, you can do that by:

```js
let descriptionListItems = [
	{
		title: "Dob",
		description: "1972-02-05",
		action: {
			label: "Update",
			href: "/callcentre/reports/reportname",
		},
	},
];
```

Where only the `label` is required, and you can add as many data as you want to `action` object, the click event will return the object.

<Story of={PoDescriptionListStories.WithAction} />

### Striped

`striped=true` default is false

<Story of={PoDescriptionListStories.Striped} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoDescriptionList
        :items="descriptionListItems"
        :striped="true"
    />
  `}
/>

### No columns

`columns=false` default is true

<Story of={PoDescriptionListStories.NoCol} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoDescriptionList
        :items="descriptionListItems"
        :columns="false"
    />
  `}
/>

### Loading

`is-loading=true` default is true

**_Important:_** When adding is loading you may want to define `itemsCount`. This is used to display the loading placeholders and the default is set to 4. The number of items to show.

<Story of={PoDescriptionListStories.Loading} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoDescriptionList
        :items="descriptionListItems"
        :is-loading="true"
    />
  `}
/>

### Adding content via slot

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoDescriptionList>
      <template v-slot:content>
        <div>
          <dt>Title</dt>
          <dd>Description</dd>
        </div>
        <div>
          <dt>Title 2</dt>
          <dd>Description 2</dd>
        </div>
        <div class="link">
          <dt>Title 3</dt>
          <dd>Description 3</dd>
        </div>
      </template>
    </PoDescriptionList>
  `}
/>

Adding .link class to description item adds a hover effect.

### Explanation

<ArgTypes of={PoDescriptionListStories} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    let descriptionListItems = [
        {
            title: "Full name",
            description: "Saleem Ahmed Doe",
        },
        {
            title: "Address",
            description: "House of Sal, Medhu St, Malé",
        },
        {
            title: "Dob",
            description: "1972-02-05",
            action: "Update",
        },
    ];
    function handleDescriptionListActionClick(item) {
        switch (item) {
            case "Update":
            alert("Update action was clicked");
            break;
        }
    }
  `}
/>
