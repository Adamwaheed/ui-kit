# Form status component

import { Story, ArgsTable, Description, Source, Canvas } from "@storybook/addon-docs";

import PoFormStatusMessage from './PoFormStatusMessage.vue';

import dedent from 'ts-dedent';

<Description of={PoFormStatusMessage} />

<Canvas withToolbar>
<div class="space-y-5">
    <Story id="form-form-status--success" />
    <Story id="form-form-status--error" />
</div>
</Canvas>

### Success
<Source
  language='js'
  dark
  format={true}
  code={dedent`
    <PoFormStatusMessage message="Thank you! The record has been created successfully!" />
  `}
/>

### Error
<Source
  language='js'
  dark
  format={true}
  code={dedent`
    <PoFormStatusMessage
        :is-error="true"
        message="Could not complete your request."
        :error-list="['NID no is required', 'Date of birth is required']"
    />
  `}
/>

### Explanation

<ArgsTable of={PoFormStatusMessage} />
