# Select Field component

import { Story, ArgsTable, Description, Source, Canvas, Meta } from "@storybook/addon-docs";

import PoSelectField from './PoSelectField.vue';

import dedent from 'ts-dedent';

<Meta title="Form/Select Field" component={PoSelectField} />

<Description of={PoSelectField} />

<Canvas withToolbar>
<div class="po-grid po-grid-cols-2 po-gap-5 po-bg-white po-p-5 po-rounded-md po-shadow-md">
    <Story inline={true} id="form-select-field--select-field" />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    &nbsp;
</div>
</Canvas>

<Source
  language='js'
  dark
  format={true}
  code={dedent`
    <PoSelectField
        label="Select something"
        :list="selectFieldList"
        :pre-selected="selectFieldPreSelected"
        @selected="handleSelectFieldClick"
        v-model="selectFieldSelected"
    />
  `}
/>

### Explanation

<ArgsTable of={PoSelectField} />

<Source
  language='js'
  dark
  format={true}
  code={dedent`
    const selectFieldSelected = ref(null);
    const selectFieldList = [
        { id: 1, name: 'Koshaaru' },
        { id: 2, name: 'HelpDesk' },
        { id: 3, name: 'Another' },
    ];
    const selectFieldPreSelected = ref(selectFieldList[1]);
    function handleSelectFieldClick(val) {
      console.log('handleSelectFieldClick', val)
    }
  `}
/>