# Select Input component

import { Story, ArgsTable, Description, Source, Canvas } from "@storybook/addon-docs";

import PoSelectField from './PoSelectField.vue';

import dedent from 'ts-dedent';

<Description of={PoSelectField} />

<Canvas withToolbar>
<div class="grid grid-cols-2 gap-5 bg-white p-5 rounded-md shadow-md">
    <Story inline={true} id="form-select-field--select-field" />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    <br />
    &nbsp;
</div>
</Canvas>

<Source
  language='js'
  dark
  format={true}
  code={dedent`
    <PoSelectField
        label="Select something"
        :list="selectFieldList"
        :pre-selected="selectFieldPreSelected"
        v-model="selectFieldSelected"
    />
  `}
/>

### Explanation

<ArgsTable of={PoSelectField} />

<Source
  language='js'
  dark
  format={true}
  code={dedent`
    const selectFieldSelected = ref(null);
    const selectFieldList = [
        { id: 1, name: 'Koshaaru' },
        { id: 2, name: 'HelpDesk' },
        { id: 3, name: 'Another' },
    ];
    const selectFieldPreSelected = ref(selectFieldList[1]);
  `}
/>
/>