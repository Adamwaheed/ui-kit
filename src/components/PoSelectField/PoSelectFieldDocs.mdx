# Select Field component

import { Story, ArgTypes, Source, Meta } from "@storybook/blocks";

import * as PoSelectFieldStories from "./PoSelectField.stories";

import dedent from "ts-dedent";

<div className="po-grid po-grid-cols-2 po-gap-5 po-bg-white po-p-5">
	<Story of={PoSelectFieldStories.SelectField} />
	<br />
	<br />
	<br />
	<br />
	<br />
	<br />
	<br />
	&nbsp;
</div>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoSelectField
        label="Select something"
        :list="selectFieldList"
        v-model="selectFieldSelected"
    />
  `}
/>

You can use `error-message` to show an error message. The field will update visually to indicate that it has an erorr. You can use `message` property to display a message to the user.
You can use the `:disabled="true"` prop to disable the field.

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    const selectFieldSelected = ref(null);
    const selectFieldList = [
        { id: 1, name: 'Koshaaru' },
        { id: 2, name: 'HelpDesk' },
        { id: 3, name: 'Another' },
    ];
  `}
/>

#### Subtitles and selected Event

You can have a sub title displayed in the select field:

<div className="po-grid po-grid-cols-2 po-gap-5 po-bg-white po-p-5">
	<Story of={PoSelectFieldStories.SelectFieldTwo} />
	<br />
	<br />
	<br />
	<br />
	<br />
	<br />
	<br />
	&nbsp;
</div>

Every time a user selects from the dropdown `selected` event is fired.

<Source
	language="js"
	dark
	format={true}
	code={dedent`
	<PoSelectField
			label="Select field"
			:list="selectFieldListTwo"
			@selected="handleSelectFieldTwoClick"
			v-model="selectFieldTwoSelected"
		/>
		
    const selectFieldSelected = ref(null);
    const selectFieldListTwo = ref([
			{ id: 1, name: "Sub category name", subtitle: "Category" },
			{ id: 2, name: "Running around in circles", subtitle: "Activity" },
			{ id: 3, name: "Something drinkable", subtitle: "Water" },
		]);

    function handleSelectFieldTwoClick(val) {
    	console.log(val)
    }

`}
/>

### Returning an OBJECT

**_ If you wish for the select field to return the item object when clicked _**
You can set the `object` prop

<div className="po-grid po-grid-cols-2 po-gap-5 po-bg-white po-p-5">
	<Story of={PoSelectFieldStories.SelectFieldThree} />
	<br />
	<br />
	<br />
	<br />
	<br />
	<br />
	<br />
	&nbsp;
</div>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
		<PoSelectField
			label="Select Object"
			object
			@selected="handleSelectFieldClick"
			:list="selectFieldListThree"
			v-model="selectFieldThreeSelected"
		/>
`}
/>

### Explanation

<ArgTypes of={PoSelectFieldStories} />
