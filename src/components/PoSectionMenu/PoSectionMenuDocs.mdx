# Section menu component

import { Story, ArgTypes, Source, Meta } from "@storybook/blocks";

import PoSectionMenu from "./PoSectionMenu.vue";
import * as PoSectionMenuStories from "./PoSectionMenu.stories";

import dedent from "ts-dedent";

**We nolonger use this. Use PoTabs intead of this. But if there is a good use case and the UX makes sense, you can use it.**

<div className="po-py-10">
	<div className="po-grid po-grid-cols-3">
		<div>
			<Story of={PoSectionMenuStories.SectionMenu} />
		</div>
		<div className="po-col-span-2"></div>
	</div>
</div>

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    <PoSectionMenu
        curr-page-route="/users"
        :menu-items="menuItems"
        @link-click="handleSectionMenuClick"
    />
  `}
/>

### Explanation

<ArgTypes of={PoSectionMenu} />

<Source
	language="js"
	dark
	format={true}
	code={dedent`
    import { UsersIcon, PuzzlePieceIcon, KeyIcon, RectangleStackIcon, BriefcaseIcon } from '@heroicons/vue/24/outline';
    const menuItems = [
        {
            label: 'Staff',
            icon: BriefcaseIcon,
            link: '/users/staff'
        },
        {
            label: 'Users',
            icon: UsersIcon,
            link: '/users'
        },
        {
            label: 'Roles',
            icon: PuzzlePieceIcon,
            link: '/users/roles'
        },
        {
            label: 'Apps',
            icon: RectangleStackIcon,
            link: '/users/apps'
        },
        {
            label: 'Permissions',
            icon: KeyIcon,
            link: '/users/permissions'
        },
    ];
    function handleSectionMenuClick(link) {
        console.log('consentlink', link)
    }
  `}
/>
